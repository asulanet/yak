---
project_slug: {{ACTIVE_PROJECT_SLUG}}
schema_version: 1
---

# Batches

<!--
Auto-written by the multi-batch lifecycle engine (session-store.js
startNewBatch) whenever a batch closes. One section per completed batch.

Each entry follows this shape:

## Batch <N> — <summary line>

- Closed: <ISO timestamp>
- Goal: <1-line>
- Outcome: <1-line>
- Key decisions: <short list>

The Phase 1 prompt of a new batch prepends this file (truncated if very long)
so scope discovery in the new batch builds on prior work.
-->
